# Terraform state files and backups
*.tfstate
*.tfstate.backup

# Crash log files
crash.log

# Sensitive variable files (optional: only if you store secrets locally)
*.tfvars
*.tfvars.json

# Terraform local directories (plugins, providers, plans)
.terraform/
**/.terraform/

# Lock file (exclude only if not using consistent provider versioning)
.terraform.lock.hcl

# Ignore any override configuration files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Plan output (if generated)
*.tfplan

# Ignore local environment and debugging settings
.terraformrc
terraform.rc

# OS/system files
.DS_Store
Thumbs.db
